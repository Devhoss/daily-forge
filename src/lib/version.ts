/** Single source of truth for the app version shown in Settings/About. */
export const APP_VERSION = '1.0.0';
/** Release phase label (RC1/RC2/GM). Kept separate from the numeric version. */
export const APP_PHASE = 'RC1';
